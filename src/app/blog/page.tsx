import { BlogIndex } from "@/components/blog/BlogIndex";

export const metadata = {
  title: "Blog | The Alter Ego",
  description: "Editorial notes on identity, motion, and headless Shopify craft.",
};

export default function BlogPage() {
  return <BlogIndex />;
}
